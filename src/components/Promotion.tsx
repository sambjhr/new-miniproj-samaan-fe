"use client";

export default function Promotion() {
  return (
    <section className="w-full flex justify-center px-4 mt-16">
      <div className="w-full max-w-6xl">
        <div className="w-full h-40 sm:h-48 bg-gray-300 rounded-xl flex items-center justify-center px-4">
          <span className="text-gray-600 text-base sm:text-lg text-center">
            Promotion Banner (Limited Offer)
          </span>
        </div>
      </div>
    </section>
  );
}
